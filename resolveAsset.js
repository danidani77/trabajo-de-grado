// ============================================================
// RESOLVEASSET — en este proyecto los archivos son reales,
// servidos por el servidor (Vercel). No se necesita convertir
// nada a Blob: la ruta relativa ya es una URL válida.
// ============================================================
function resolveAsset(path){
  return path;
}
